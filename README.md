# Структура проекта Ball Game

## Описание структуры

Проект организован по принципу разделения ответственности (Separation of Concerns) с использованием feature-based архитектуры.

### 📁 Структура папок

```
lib/
├── main.dart                          # Точка входа приложения
├── core/                              # Ядро приложения (общие компоненты)
│   ├── constants/                     # Константы
│   │   └── game_constants.dart        # Константы игры
│   └── models/                        # Модели данных
│       └── game_state.dart            # Состояние игры (enum)
└── features/                          # Функциональные модули
    └── game/                          # Игровая функциональность
        ├── ball_game.dart             # Основной класс игры (Flame)
        ├── components/                # Игровые компоненты
        │   ├── ball_component.dart    # Компонент мячика
        │   ├── platform_component.dart # Компонент платформы
        │   ├── coin_component.dart    # Компонент монеты
        │   └── spike_component.dart   # Компонент шипов
        ├── screens/                   # Экраны приложения
        │   ├── game_screen.dart       # Главный экран игры
        │   └── game_over_screen.dart   # Экран окончания игры
        └── widgets/                   # Виджеты игры
            ├── control_button.dart    # Виртуальная кнопка управления
            ├── game_header.dart       # Заголовок игры
            └── game_status_overlay.dart # Оверлей статуса игры
```

## Компоненты

### Core (Ядро)
- **constants/game_constants.dart** - Все константы игры (скорости, размеры, цвета, физические параметры)
- **models/game_state.dart** - Enum состояний игры (playing, won, lost)

### Features/Game (Игровая функциональность)

#### Game Logic (Игровая логика)
- **ball_game.dart** - Основной класс игры, управляет игровым циклом, компонентами, физикой и логикой победы/поражения

#### Components (Игровые компоненты)
- **ball_component.dart** - Компонент мячика с физикой движения, столкновениями и управлением
- **platform_component.dart** - Компонент платформы для отскоков мячика
- **coin_component.dart** - Компонент монеты для сбора
- **spike_component.dart** - Компонент шипов (опасная зона)

#### Screens (Экраны)
- **game_screen.dart** - Главный экран игры, содержит логику управления клавиатурой и виртуальными кнопками
- **game_over_screen.dart** - Экран окончания игры (победа/поражение)

#### Widgets (Виджеты)
- **control_button.dart** - Переиспользуемая виртуальная кнопка управления
- **game_header.dart** - Заголовок игры с кнопкой перезапуска
- **game_status_overlay.dart** - Оверлей для отображения очков и прогресса сбора монет

## Принципы организации

1. **Feature-based архитектура** - Код организован по функциональным модулям (features)
2. **Разделение ответственности** - Каждый файл отвечает за свою область
3. **Переиспользуемость** - Виджеты и компоненты можно легко переиспользовать
4. **Централизация констант** - Все магические числа вынесены в отдельный файл
5. **Модульность** - Легко добавлять новые компоненты и функции
6. **Масштабируемость** - Структура позволяет легко добавлять новые features

## Преимущества новой структуры

- ✅ Четкое разделение между core (общие компоненты) и features (функциональность)
- ✅ Легко найти нужный файл по его назначению
- ✅ Удобно добавлять новые features (например, `features/menu/`, `features/settings/`)
- ✅ Общие компоненты (константы, модели) доступны из любого места
- ✅ Компоненты, экраны и виджеты логически сгруппированы внутри feature
